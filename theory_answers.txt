############ Задание 1

1) всего нужно 5 таблиц: players, enemies, fights, fight_enemies, fight_players, fight_logs
players - лист всех зареганых игроков, со статами
enemies - лист всех противников в игре, со статами,  в т.ч и босса о котором речь
fight - битва, хранит данные об участниках битвы, собственно массивы с id игроков и id врагов
fight_enemies - хранит id битвы, и текущие статы участников битвы[врагов], т.е изначально туда пишутся статы из таблицы enemies , но по ходу боя они меняются
fight_player - аналогично fight_enemies только с игроками 
fight_logs - хранит id битвы, и все события в строковом виде которые происходили в битве

собственно взаимодействие происходит через класс Fight, который содержит все нужные данные из 5ти таблиц, в свойх свойствах, массивы Player\Enemy обьектов и тд, его можно закешировать чтобы каждый раз кучу инфы не доставать из БД. ну собственно в самом классе есть бесконечный секундный таймер например, который дергает Enemy->do($this) какой-нибудь, который решает что в текущий момент делает враг, и тд и тп.     


2) "НЕ терялся" полагаю? - хранить текущее состояние в таблице, таблицу в БД, а для БД бекап\реплику :) , таблица fight_enemies как раз хранит текущее состояние объекта в битве. если упадет memcached - можно будет по новой построить ход битвы по ней.

3) всем игрокам - перебрать массив игроков из битвы и декрементировать каждому health. массив игроков прилетает с объектом Fight (см. 1 ответ)
по ста сильным игрокам - написать метод в Enemy, который из массива игроков выбирает 100 сильнейших и декрементирует им health.

4) вопрос не понятен. да хоть все, что хочешь. логи битвы? чат\повреждения\статистику

############ Задание 2

1) индексация created поля (datetime). кеширование отчетов

2) вопрос не понятен. запрашивать на клиенте ассинхронно?

############ Задание 3

1) создание бекапов, настройка репликации

2) если реплицация - данные будут последней свежести, думаю репликация лучше

3) при репликации вроде полный клон базы получется - можно сделать mysqldump реплики и залить на новый мастер. при бекапе - смотря как бекапы реализованы.
Но если не с чего восстанавливать - тут вперед гуглить утилиты по васстановлению горелых дисков или сервис отнести, если винт на корпоративе прям уронили в мангал с шашлычками  

